pipeline {
    agent any

    stages {
        stage('clone repositoy') {
            steps {
                git 'https://github.com/learndevops-083/samplejavaapp.git'
            }
        }
        
        stage('maven package') {
            steps {
                sh '/opt/maven/bin/mvn package'
            }
        }
    }
  }
